Windows сервис, выполняющий копирование данных раз в минуту (частота резервных копий указывается в исходном коде, путь до файлов-в конфигурационном файле. 

Использует библиотеку zlib для архивирования, установленную через NuGet
